node_modules/
.next/
dist/
build/
*.tsbuildinfo
.env
.env.local
.env.*.local
!.env.example
.DS_Store
coverage/
uploads/
*.log
npm-debug.log*
.turbo/

# A local database dump. Committing one would put content — and any lead that was in it when
# the dump was taken — into the repository's history, where deleting the file later does not
# remove it.
db/
dump/
*.bson
